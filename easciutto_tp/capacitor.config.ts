import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'easciutto_tp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
